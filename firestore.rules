rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {

    // クライアントからの直接アクセスをすべて禁止
    // アプリは Firebase Admin SDK（サーバー側）経由でのみアクセスする
    // Admin SDK はこのルールをバイパスするため、アプリの動作には影響しない
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
